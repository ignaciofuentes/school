
<% if employees.nil? or employees.empty? %>
  <div id="no_result">No Employee found</div>
<% else %>
  <% employees.each do |r| %>
    <div id="ajax_result">
      <%= link_to r.full_name, r %><br/>
      <small>Emp ID.<%= r.employee_number %></small>
    </div>
  <% end %>
<% end %>
