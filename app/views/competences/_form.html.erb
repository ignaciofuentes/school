<%= nested_form_for([@batch_subject, @competence]) do |f| %>
<div class ="container_12">
	<h2></h2>
	<% if @competence.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@competence.errors.count, "error") %> prohibited this competence from being saved:</h2>

		<ul>
			<% @competence.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
	<% end %>
</div>

<div class="clearfix">&nbsp;</div>

<div class ="container_12">
	<div class="grid_5 alpha">
		<p>
			<%= f.label :period_id %>
			<%= f.collection_select :period_id, @periods, :id, :name, :class=>"select" %>
		</p>
		<p>
		<%= f.label :description %>
		<%= f.text_area :description, :class=>"small" %>
		</p>
		
	</div>
	<div class="grid_7 omega">
	<%= f.fields_for :indicators do |builder|%>
	<%= render "indicator_fields", :f=> builder%>
	<%end%>
	<%= f.link_to_add "Add Indicator", :indicators%>
	</div>
</div>

<div class="clearfix">&nbsp;</div>
<hr />
<div class="container12">
	<%= f.submit :value => "► Guardar Competence", :class => "submit"  %>
</div>
<% end %>

