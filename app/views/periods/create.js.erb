<%if @period.new_record?%>
	$("#form-errors").html('<%= escape_javascript(render 'shared/error_messages', :target => @period) %>');
	$("#modal-box").dialog("open");
<% else %>
		$("#periods-list-body").append('<%= escape_javascript(render @period ) %>');		
		$("#new_period")[0].reset();
<% end %>